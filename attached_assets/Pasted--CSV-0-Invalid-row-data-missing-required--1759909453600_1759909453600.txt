[사전 변경 안내]
수정 목적(한 줄)
- 접점코드 및 서비스플랜 CSV/엑셀 업로드 시 "0개 업로드" 또는 "Invalid row data (missing required fields)"가 발생하는 문제의 근본 원인(컬럼명 불일치)을 정확히 진단하기 위해, 실제 CSV/엑셀의 1행 컬럼명을 서버 로그로 출력하는 최소 변경(로깅만) 작업을 수행합니다. DB/스키마/환경변수/UI 변경 없음.

해결할 오류 / 재현 절차
- 관리자 → 접점코드 관리 → 업로드: 0개 처리
- 관리자 → 서비스플랜 → 업로드: "Invalid row data ..."
- 재현: CSV/엑셀 업로드 시 실제 컬럼명을 서버가 기대하는 컬럼명과 매칭하지 못함 → 컬럼명 로깅으로 정확한 불일치 지점 확인 필요

변경 범위 요약
- 백엔드: server/routes.ts (단일 파일)
  - 공통 유틸 2개 함수 추가 (헤더 추출/로깅)
  - 접점코드 업로드 엔드포인트에 헤더 로깅 4줄 추가
  - 서비스플랜 업로드 엔드포인트에 헤더 로깅 4줄 추가
- 프론트/빌드/배포/환경변수: 변경 없음 (운영 시 필요하면 LOG_CSV_HEADERS=true 로 켜고 끌 수 있도록만 처리)

요구 사항(절대 준수)
- 기존 업로드/검증/DB 저장 로직은 일절 수정하지 말 것
- "로깅만" 추가. 실패해도 본 로직에 영향 없도록 try/catch로 감싸기
- 운영(production)에서는 기본 비활성, 개발에서 기본 활성
- 메모리/디스크 어떤 업로드 방식이든 동작하도록 구현 (req.file.buffer 우선, 없으면 fs.readFileSync(req.file.path))

[수정 지시]
파일: server/routes.ts

1) 상단 import 주변에 다음 유틸 추가(중복 import 금지)
- XLSX 이미 import 되어 있으면 재-import 금지
- fs는 필요 시 새로 import

추가 코드:
----------------------------------------------------------------
import * as fs from "fs";
import * as XLSX from "xlsx"; // 이미 있으면 중복 추가 금지

// 운영에서는 기본 비활성, 개발/테스트에서는 기본 활성
const LOG_CSV_HEADERS =
  (process.env.LOG_CSV_HEADERS ?? "").toLowerCase() === "true" ||
  (process.env.APP_ENV ?? "development") !== "production";

/** 업로드된 파일의 첫 번째 시트에서 헤더(1행)만 추출 */
function extractCsvHeadersFromUploadedFile(file: Express.Multer.File): string[] {
  if (!file) return [];
  let buffer: Buffer | null = null;

  if (file.buffer && file.buffer.length > 0) {
    buffer = file.buffer as Buffer;
  } else if ((file as any).path) {
    buffer = fs.readFileSync((file as any).path);
  }
  if (!buffer) return [];

  const wb = XLSX.read(buffer, { type: "buffer" });
  const ws = wb.Sheets[wb.SheetNames[0]];
  const aoa = XLSX.utils.sheet_to_json(ws, { header: 1, defval: "" }) as any[][];
  const headers = (aoa?.[0] ?? []).map((h) => String(h ?? "").trim());
  return headers;
}

/** 조건부 로깅: 운영 기본 OFF, LOG_CSV_HEADERS=true일 때만 ON */
function logCsvHeaders(label: string, headers: string[]) {
  if (!LOG_CSV_HEADERS) return;
  const normalized = headers.map((h) => h.replace(/\s+/g, " ").trim());
  console.log(`📊 CSV 컬럼명[${label}]: ${JSON.stringify(normalized)}`);
}
----------------------------------------------------------------

2) "접점코드 업로드" 엔드포인트 상단(파일 존재 확인 직후)에 헤더 로깅 4줄 추가
- 라우트 경로 예시는 다를 수 있으므로, 접점코드 업로드를 처리하는 POST 엔드포인트를 찾아 아래 블록을 삽입

삽입 코드(파일 존재 확인 후 바로 아래):
----------------------------------------------------------------
try {
  const headers = extractCsvHeadersFromUploadedFile(req.file as any);
  logCsvHeaders("접점코드", headers);
} catch (e) {
  if (LOG_CSV_HEADERS) console.warn("[CSV][접점코드] 헤더 추출 실패:", e);
}
----------------------------------------------------------------

3) "서비스플랜 업로드" 엔드포인트에도 동일하게 4줄 추가
- 서비스플랜 업로드 POST 엔드포인트를 찾아 동일 블록 삽입

삽입 코드:
----------------------------------------------------------------
try {
  const headers = extractCsvHeadersFromUploadedFile(req.file as any);
  logCsvHeaders("서비스플랜", headers);
} catch (e) {
  if (LOG_CSV_HEADERS) console.warn("[CSV][서비스플랜] 헤더 추출 실패:", e);
}
----------------------------------------------------------------

[검증 시나리오 & 성공 기준]
- 개발 환경(APP_ENV=development)에서 업로드 재시도:
  - 콘솔 로그에 아래 형태가 1회 출력되어야 함
    - 📊 CSV 컬럼명[접점코드]: ["접점코드","지점명",...]
    - 📊 CSV 컬럼명[서비스플랜]: ["요금제명","통신사",...]
- 본 로직은 그대로 동작(HTTP 200 응답 유지). 업로드 실패/성공 여부는 기존 로직대로
- 출력된 실제 컬럼명을 근거로 이후 매핑 보정 작업을 진행 가능

[리스크와 회피책]
- 성능 영향 최소화: 1행만 파싱/로깅, 운영 기본 OFF
- 실패 시 로깅만 건너뜀(본 처리 영향 없음)

[롤백 계획]
- server/routes.ts에서 추가한 유틸 함수와 4줄 로깅 블록 제거
- 또는 git revert

[추가 메모]
- 이번 변경은 "진단용 로깅"만 포함합니다. 컬럼명 매핑 보정은 별도 변경으로 분리합니다.