[수정 원칙 - 필수]

오류가 발생한 부분만 최소 수정

UI 레이아웃/디자인/스타일/컴포넌트 구조 전반은 유지
※ 본 건은 사용자 요청에 따라 **테이블 컬럼(번호 제거, 순서 변경)**만 제한적으로 변경

버튼 색상, CSS, Tailwind 클래스, HTML 구조는 기존 유지

환경설정(.env, Secrets, DB 연결, 운영 DB 로직) 절대 변경 금지

DB 스키마 변경 금지(필요 시 ‘DB 스키마 변경 제안’ 섹션으로만 설명)

부분 코드 금지 → 파일 전체 최종본 제출

실제 오류 재현 후 필요한 최소 범위만 수정

수정 후 서버 재시작 및 동일 시나리오 재검증

운영/테스트 동일 커밋·동일 번들로 배포

변경 개요

수정 목적:

테이블의 번호 컬럼 제거 및 개통완료시간을 첫 번째 컬럼으로 이동

전화번호 필드 매핑 수정(현재 필드 불일치로 공백 표시)

개통처리자 매핑 로직 개선(ID→이름 매칭 실패 해결)

해결할 오류 / 재현 절차(간단)

현상 1: 테이블 첫 컬럼이 번호로 잡혀 있음 (불필요)

현상 2: 전화번호가 비어 있거나 -로 표시됨

현상 3: 처리자 컬럼이 비어 있거나 -로 표시됨

재현: /completed-activations 페이지 진입 → 테이블 확인

원인 추정

번호: 단순 인덱스 표시 컬럼 잔존

전화번호: (doc as any).phoneNumber로만 접근 → 실제 응답 필드와 불일치

처리자: getActivatedByNameSync(doc)에서 activated_by 타입/키 불일치(숫자↔문자열, null) 또는 사용자 목록 매칭 누락

변경 범위 요약

프론트엔드만 수정

테이블 컬럼 배열 및 매핑만 조정

데이터 매핑(전화번호/처리자) 안전 폴백 체인 추가

백엔드/빌드/배포/환경변수/스타일: 변경 없음(빌드·배포는 실행 필요)

수정 파일 목록 (최소화)

client/src/pages/CompletedActivations.tsx

테이블 헤더/바디 컬럼 구성 변경(번호 제거, 개통완료시간 1열 이동)

전화번호 필드 폴백 체인 적용

처리자 매핑 함수(내부 유틸 또는 동일 파일 함수) 타입 안전/폴백 보강

파일 경로 + 파일별 변경 이유

CompletedActivations.tsx

불필요한 번호 컬럼 제거 및 개통완료시간을 사용자가 가장 먼저 인지할 수 있도록 첫 번째 컬럼으로 이동

응답 스키마 차이로 인한 전화번호 공백을 방지하기 위해 다중 필드 폴백 체인 적용

예시 체인(설명용): doc.phoneNumber → doc.customerPhone → doc.contact → doc.customer_phone → '-'

getActivatedByNameSync(혹은 동등 기능)에서

activated_by가 null/undefined면 '-'

숫자/문자열 ID 양쪽 매칭 지원

사용자 목록 부재 시 안전 폴백 '-'

예상 변경 내용 (요약 diff)

[삭제] <th>번호</th> 및 <td>{index + 1}</td>

[이동] 개통완료시간 컬럼을 헤더/바디 모두 첫 번째 위치로

[치환] 전화번호 접근:

기존: (doc as any).phoneNumber

변경: 다중 폴백 체인으로 안전 접근(위 예시 체인)

[개선] 처리자 매핑:

activated_by가 null/'' → '-'

users.find(u => String(u.id) === String(activated_by)) 방식으로 타입 불일치 보정

매칭 실패 시 '-'

스타일/클래스/컴포넌트 구조는 그대로 유지. 컬럼 구성(번호 제거/순서 변경)만 반영.

환경변수/DB/스키마/스타일 — 미변경

.env/Secrets/DB 연결/스키마/CSS/Tailwind/레이아웃 변경 없음

리스크와 회피책

잠재 영향: 컬럼 순서 변경에 따른 일시적 시각적 차이

회피/완화:

기존 CSS/클래스 유지로 레이아웃 영향 최소화

빌드 후 즉시 동일 시나리오 재검증

문제 시 단일 커밋 롤백

롤백 계획

현재 커밋 ID 기록

문제 시 git revert <commit> 또는 CompletedActivations.tsx 원복

서버 재시작으로 즉시 복귀

검증 시나리오 & 성공 기준

컬럼 순서: 개통완료시간이 첫 번째 컬럼에 렌더링 (번호 컬럼 미표시)

전화번호: 각 행에서 실제 값 정상 표기(공백/- 사라짐)

처리자: 각 행에서 처리자명이 올바르게 표기(미매칭 시 '-'), 콘솔 에러 0

엔드투엔드 3건:

페이지 로드(200, 콘솔 에러 0)

필터/페이지 이동 후에도 일관 표시

운영/테스트 동일 결과(동일 커밋/번들)

성공 조건: 콘솔 에러 0개, 테이블 모든 행에서 개통완료시간/전화번호/처리자 정상 렌더링

[제출 규칙]

본 사전 변경 안내 승인 전, 코드 본문 제시 금지

승인 후 파일 전체 최종본만 제출(부분 코드 금지)

수정 전/후 결과를 로그·스크린샷으로 검증해 함께 제출