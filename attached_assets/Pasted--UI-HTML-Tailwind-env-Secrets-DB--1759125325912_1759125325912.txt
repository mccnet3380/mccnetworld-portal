[수정 원칙 - 필수]

오류 지점만 최소 수정, 불필요 변경 금지

UI/레이아웃/디자인/스타일/HTML/Tailwind 절대 변경 금지

.env/Secrets/DB 연결/운영 DB 로직 절대 수정 금지

DB 스키마 변경 금지 (필요 시 제안만)

부분 코드 금지 → 파일 전체 최종본 제출

서버 재시작 및 동일 시나리오 재검증 필수

운영/개발 동일 커밋·동일 번들로 배포

변경 개요

운영 사이트: 정적 파일 서빙/라우팅 오류로 /assets/*가 500 또는 MIME(type: application/json) 로 응답 → 빈페이지

콘솔 재현:

Failed to load resource: the server responded with a status of 500 index-Cj1dU44t.js:1

Refused to apply style ... index-CA0Ty6AC.css because its MIME type ('application/json') is not a supported stylesheet MIME type

개발 페이지: 최신 번들 미로딩(캐시/서비스워커/HMR 문제)로 /documents에서 빈페이지 간헐 발생

수정 목적(한 줄)

운영의 정적 자산 라우팅/MIME을 정상화하고, 개발의 번들 캐시/HMR을 무효화하여 /documents가 정상 렌더되도록 복구.

해결할 오류 / 재현 절차(간단)

운영: https://mccnetworld.com/documents

증상: 빈페이지, 위 500/MIME 에러

원인: /assets/*가 API 라우터/캐치올로 흘러가 JSON 응답 또는 500 발생(정적 미서빙)

개발: …replit.dev/documents

증상: 빈페이지, 과거 번들/캐시 사용 가능성

변경 범위 요약

백엔드(서버 라우팅 순서/정적 서빙만 수정) + 빌드/캐시 무효화

프론트 코드/스타일/컴포넌트/DB/환경설정 미변경

수정 파일 목록 (최소화)

server/index.ts(또는 서버 진입 파일): 정적 자산 서빙 + 라우팅 순서 보정

(필요 시) server/routes.ts: catch-all 라우팅 위치 조정

코드 외 작업: dist 재빌드, SW/브라우저 캐시 무효화

파일 경로 + 파일별 변경 이유

server/index.ts

/assets/* 및 /<hash>.js/.css 등 정적 자산을 express.static으로 우선 서빙하고,

그 다음에 API 라우터, 마지막에 SPA catch-all(app.get('*', send index.html)) 적용되도록 미들웨어 순서 수정

정적 응답의 MIME 자동 지정(express.static 기본) 및 캐시 헤더 설정

server/routes.ts (선택)

SPA catch-all이 /assets/*를 가로채지 않도록 가장 마지막으로 이동

예상 변경 내용 (요약 diff)

[추가/치환]

app.use('/assets', express.static(path.resolve(__dirname, '../client/dist/assets')))

app.use(express.static(path.resolve(__dirname, '../client/dist'))) // index.html 포함

app.use('/api', apiRouter) // 정적보다 뒤 X, 정적 → API → catch-all 순서 준수

app.get('*', (_req,res)=>res.sendFile(path.resolve(__dirname,'../client/dist/index.html'))) // 맨 마지막

[유지] 프론트/스타일/UI 로직은 변경 없음

※ 실제 경로는 프로젝트 구조에 맞춰 ../client/dist 등으로 정확히 지정.

핵심 포인트 (추가/삭제/치환)

정적 서빙을 최우선으로 두고, API, 마지막에 SPA catch-all

/assets/*가 JSON 200/500으로 응답하지 않도록 라우팅 순서 고정

빌드/캐시 무효화로 구 해시 번들 제거(dist 재생성, 서비스워커 미사용/해제)

환경변수/DB/스키마/스타일은 미변경

.env/Secrets/DB/스키마/UI/스타일/레이아웃: 변경 없음

리스크와 회피책

리스크: 라우팅 순서 변경 시 API/SPA 경로 충돌

회피: 정적 → API → catch-all 순서 명시 및 배포 전후 라우팅 로그로 확인

롤백 계획

변경 전 server/index.ts/server/routes.ts 백업

문제 시 해당 파일 원복 또는 git revert <commit>

서버 재시작으로 즉시 복귀

검증 시나리오 & 성공 기준

운영 https://mccnetworld.com/

Network: index.html 200, /assets/index-.js / index-.css 200 & MIME 정확

콘솔 에러 0, 화면 정상 렌더

운영 https://mccnetworld.com/documents

테이블 렌더 OK, 콘솔 에러 0

개발 …replit.dev/documents

dist 재빌드 후 새 해시 번들 로드 확인(Network JS 목록)

콘솔 에러 0

공통

접수일시 포맷: MM/dd HH:mm (초 없음)

성공 조건: HTTP 200, 정적 파일 MIME 정상, 빈페이지 재현 불가

빌드/배포 체크리스트(담당자용)

rm -rf dist node_modules/.vite && npm ci && npm run build

dist/manifest.json의 새 index-<NEW_HASH>.js/css 확인

서버 완전 재시작 (hot reload 금지)

브라우저 Disable cache + 강력 새로고침, Service Worker 전부 unregister

index.html이 새 해시를 참조하는지 Network로 확인

증빙으로 아래 캡처를 함께 제출해 주세요.

Network: index.html, index-*.js, index-*.css 200 & MIME 정상(text/javascript, text/css)

Console: 에러 0

manifest.json: 새 해시 파일명