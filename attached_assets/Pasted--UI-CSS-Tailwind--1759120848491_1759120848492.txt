[수정 원칙 - 필수]

오류가 발생한 부분만 수정 (불필요한 변경 금지)

UI(레이아웃/디자인/스타일/컴포넌트 구조) 절대 변경 금지

버튼 색상, CSS, Tailwind 클래스, HTML 구조는 그대로 유지

환경설정(.env, Secrets, DB 연결, 운영 DB 로직) 절대 수정 금지

DB 스키마 변경 금지 → 필요 시 “코드 수정 없이” ‘DB 스키마 변경 제안’으로만 설명

부분 코드 금지 → 파일 전체 최종본 제출

즉시 복붙 가능해야 함 (불필요한 설명 최소화)

추측/가정 금지 → 실제 오류 재현 후 필요한 최소 범위만 수정

⚠️ 수정 후 서버 재시작 및 동일 시나리오 재검증 필수

⚠️ 코드 제출 전/후 동일 시나리오에서 오류 없는지 재확인

⚠️ 운영 페이지와 테스트 페이지 동일하게 코드 수정할 것

변경 개요

수정 목적: Documents.tsx에서 누락된 canExportToExcel로 인한 JS 에러 해소 및 접수일시 표시 형식을 MM/dd HH:mm(분까지)로 통일.

해결할 오류 / 재현 절차(간단)

개발/운영 – 접수 관리 페이지 빈페이지

증상: 콘솔에 canExportToExcel is not defined → 렌더 실패

기대: 함수 존재, 페이지 정상 렌더

접수일시 형식

증상: 초까지 표기 또는 NaN 표기

기대: MM/dd HH:mm(예: 09/29 14:30)로만 표시, 유효하지 않은 값은 '-' 표시

운영 사이트 빈페이지(정적 파일)

현 상태: 백엔드 정적 서빙 설정은 이미 적용됨(변경 불필요)

본 요청: 프론트 오류 제거로 정상 로드 확인

변경 범위 요약

프론트엔드만 수정

Documents.tsx에 누락 함수 추가 + 날짜 포맷 함수 수정

백엔드/DB/환경설정/스타일/레이아웃/컬럼 순서 변경 없음

수정 파일 목록 (최소화)

client/src/pages/Documents.tsx

파일 경로 + 파일별 변경 이유

Documents.tsx

canExportToExcel 추가: 권한 체크 함수 누락으로 런타임 에러 발생 → 동일 패턴으로 복원

formatReceptionDateTime 수정: 초 단위 제거, 파싱 가드 추가로 NaN 방지

예상 변경 내용 (요약 diff)

[추가] function canExportToExcel(user) { /* 기존 권한 패턴과 동일 */ return hasRole(user,'admin') || hasPermission(user,'export_documents'); }

[치환] format(date, 'yyyy-MM-dd HH:mm:ss') → format(validDate, 'MM/dd HH:mm')

[추가] 안전 파싱 가드:

문자열 ISO → parseISO

number/Date → new Date(value)

유효성 실패 시 '-' 반환(콘솔 에러 금지)

핵심 추가/치환 포인트

누락 함수 복원: canExportToExcel 만 추가

표시 포맷 통일: MM/dd HH:mm

안전 가드: 잘못된 날짜는 '-' 처리

기존 권한/스타일/레이아웃/필터/정렬 로직은 절대 변경 금지

환경변수/DB/스키마/스타일 미변경

.env / Secrets: 변경 없음

DB 연결/스키마: 변경 없음

CSS/Tailwind/UI 스타일/레이아웃: 변경 없음

리스크와 회피책

권한 분기 오판 위험: 버튼 노출/비노출 이슈 가능 → 기존 권한 체크 패턴 그대로 재사용

표시 포맷 불일치: 다른 화면과 차이 가능 → 본 화면 한정 적용, 원본 데이터 보존

롤백 계획

수정 전 Documents.tsx 백업

문제 시 해당 파일만 이전 버전으로 복원(또는 git revert <commit>)

서버 재시작으로 즉시 반영 취소

검증 시나리오 & 성공 기준

페이지 로드: /documents 접속 → 빈페이지 아님, 콘솔 에러 0

접수일시 포맷: 테이블 ‘접수일시’가 MM/dd HH:mm 형식(예: 09/29 14:30)

엑셀 버튼 노출: 권한 있는 계정에서 버튼 표시/다운로드, 권한 없는 계정에서 비표시

운영/개발 동등성: 두 환경 동일 결과 확인(스크린샷)

성공 조건:

HTTP 200, 콘솔 오류 0

접수일시 포맷 반영 완료

Excel 버튼 권한 동작 정상

운영/개발 동일 동작 확인

제출 규칙

본 사전 변경 안내 승인 전 코드 본문 금지