[수정 원칙 - 필수]

오류가 발생한 부분만 수정 (불필요한 변경 금지)

UI(레이아웃/디자인/스타일/컴포넌트 구조) 절대 변경 금지

버튼 색상, CSS, Tailwind 클래스, HTML 구조 그대로 유지

환경설정(.env, Secrets, DB 연결, 운영 DB 로직) 절대 수정 금지

DB 스키마 변경 금지 → 필요 시 “코드 수정 없이” ‘DB 스키마 변경 제안’ 섹션으로만 설명

부분 코드 금지 → 파일 전체 최종본 제출

즉시 복붙 가능하게 제출(불필요한 설명 금지)

추측/가정 금지 → 실제 오류 재현 후 필요한 최소 범위만 수정

⚠️ 수정 후 서버 재시작 및 동일 시나리오 재검증 필수

⚠️ 코드 제출 전/후 같은 시나리오에서 오류 없는지 재확인

⚠️ 운영/테스트 동일 커밋·동일 번들로 배포

변경 개요

수정 목적:

개통완료 시간이 표기되지 않는 문제를 해결(분까지 표시)

고객명 클릭 → 정보 수정 수행 시 빈페이지가 발생하는 런타임 오류를 제거

해결할 오류 / 재현 절차(간단)

재현 1: 접수 관리 → 개통완료 이동 → 테이블의 개통완료시간 컬럼이 비어 있음

기대: activated_at 우선, 없으면 uploaded_at로 MM/dd HH:mm 형식 표시

재현 2: 개통완료 문서에서 고객명 클릭 → 수정 → 화면 전체가 하얀 페이지(빈페이지)

기대: 수정 다이얼로그/페이지 정상 렌더, 200 응답

변경 범위 요약

프론트엔드만 수정 (데이터 매핑/포맷팅/에러가드/모달 마운트 가드)

백엔드/DB/환경설정/스타일/레이아웃 변경 없음

수정 파일 목록 (최소화)

client/src/pages/CompletedActivations.tsx

client/src/components/DocumentEditDialog.tsx (실제 프로젝트 경로에 맞춰 동일 파일명 전체본 제출)

파일별 변경 이유 (각 1–2줄)

CompletedActivations.tsx

개통완료시간이 activated_at만 바라봐 비어 나오는 케이스 → activated_at || uploaded_at 폴백 + MM/dd HH:mm 포맷 적용 필요

고객명 클릭 시 선택 문서/비동기 로딩 상태에서 undefined 접근으로 렌더 크래시 → null 가드/조건부 렌더 필요

DocumentEditDialog.tsx

다이얼로그 마운트 시점/props null 접근/비동기 로드 에러 미처리로 흰 화면 → try/catch + 로딩/에러 가드 필요

예상 변경 내용 (요약 diff)

[치환] 시간 포맷팅

format(date,'yyyy-MM-dd HH:mm:ss') → format(validDate,'MM/dd HH:mm')

[추가] 시간 소스 폴백

const dt = row.activated_at || row.uploaded_at;

유효성 검사 후 포맷, 실패 시 '-'

[추가] 안전 렌더 가드

고객명 클릭 핸들러: setSelected(doc) 이후에만 다이얼로그 렌더

JSX: {selectedDoc && <DocumentEditDialog .../>}

[강화] 수정 다이얼로그 내 비동기/에러 처리

await getDocument(id) try/catch → 실패 시 토스트 + 다이얼로그 닫기(흰화면 방지)

모든 중첩 접근에 ?. 사용

UI/레이아웃/스타일/클래스/컬럼 구조 변경 없음.
표시 포맷과 런타임 가드만 추가/치환.

핵심 추가/삭제/치환 포인트

개통완료시간: activated_at || uploaded_at → MM/dd HH:mm

빈페이지 방지: 선택 문서 null 가드, 다이얼로그 비동기 에러 가드

API/라우팅/컴포넌트 구조 변경 없음

환경변수/DB/스키마/스타일 미변경

.env / Secrets / DB 연결 / 스키마 / CSS / Tailwind / HTML / 컬럼 구성 → 변경 없음

리스크와 회피책

잠재 영향(성능): 가벼운 포맷/가드 추가로 영향 미미

완화: 기존 코드 경로 유지, 가드만 추가. 실패 시 '-' 안전 표기/토스트 처리

롤백 계획

수정 전 CompletedActivations.tsx, DocumentEditDialog.tsx 백업

문제 시 두 파일 원복 또는 git revert <commit>

서버 재시작으로 즉시 복귀

검증 시나리오 & 성공 기준

개통완료 목록

“개통완료시간”이 MM/dd HH:mm 형식으로 표시

activated_at 없는 항목은 uploaded_at 기준으로 표시, 둘 다 없으면 '-'

고객명 클릭 → 수정

다이얼로그/수정 화면 정상 열림

데이터 로드 실패 시 흰 화면 없음, 토스트 노출 후 닫힘

엔드투엔드 3건

로그인 → 개통완료 진입 → 시간 표기 확인

고객명 클릭/수정 저장 200 응답

목록 리프레시 후 반영 확인
성공 조건: HTTP 200, 콘솔 에러 0, 빈페이지 재현 불가, UI/레이아웃 변화 없음