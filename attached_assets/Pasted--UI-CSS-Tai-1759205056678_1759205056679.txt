[수정 원칙 - 필수]

오류가 발생한 부분만 수정 (불필요한 변경 금지)

UI(레이아웃/디자인/스타일/컴포넌트 구조)는 절대 변경 금지

버튼 색상, CSS, Tailwind 클래스, HTML 구조는 그대로 유지

환경설정(.env, Secrets, DB 연결, 운영 DB 로직)은 절대 수정 금지

DB 스키마 변경은 금지 → 필요 시 “코드 수정 없이” ‘DB 스키마 변경 제안’ 섹션으로 설명만 제시

부분 코드 금지 → 반드시 “파일 전체 최종본” 제출

내가 즉시 복붙 가능해야 함 (불필요한 설명 최소화)

추측/가정 금지 → 실제 오류 재현 → 필요한 최소 범위만 수정

⚠️ 수정 후 반드시 서버 재시작 및 동일 시나리오 재검증

⚠️ 코드 제출 전후로, 같은 시나리오에서 오류 없는지 다시 확인

⚠️테스트페이지와 운영서버(mccnetworld.com)에 대해 코드 정확하게 수정 할것


내용

1.테스트페이지내에 개통완료 관리에서 고객 정보 수정 클릭시 요금제 변경 드롭박스 없음.


수정 목적(한 줄)

해결할 오류 / 재현 절차(간단)

변경 범위 요약 (백엔드/프론트/빌드/배포 등)

수정 파일 목록 (최소화)

파일 경로 + 파일별 변경 이유 (각 1-2줄)[수정 원칙 - 필수]

오류가 발생한 부분만 수정 (불필요한 변경 금지)

UI(레이아웃/디자인/스타일/컴포넌트 구조)는 절대 변경 금지

버튼 색상, CSS, Tailwind 클래스, HTML 구조는 그대로 유지

환경설정(.env, Secrets, DB 연결, 운영 DB 로직)은 절대 수정 금지

DB 스키마 변경은 금지 → 필요 시 “코드 수정 없이” ‘DB 스키마 변경 제안’ 섹션으로 설명만 제시

부분 코드 금지 → 반드시 “파일 전체 최종본” 제출

내가 즉시 복붙 가능해야 함 (불필요한 설명 최소화)

추측/가정 금지 → 실제 오류 재현 → 필요한 최소 범위만 수정

⚠️ 수정 후 반드시 서버 재시작 및 동일 시나리오 재검증

⚠️ 코드 제출 전후로, 같은 시나리오에서 오류 없는지 다시 확인

⚠️테스트페이지와 운영서버(mccnetworld.com)에 대해 코드 정확하게 수정 할것


내용


1.업무요청중 카테고리엔 접수 관리에서 업무 요청중 상태로만 변경시 업무 요청중으로 넘어갈것 그외 다른 처리 건은 넘어 오면 안됨.


수정 목적(한 줄)

해결할 오류 / 재현 절차(간단)

변경 범위 요약 (백엔드/프론트/빌드/배포 등)

수정 파일 목록 (최소화)

파일 경로 + 파일별 변경 이유 (각 1-2줄)

예상 변경 내용 (요약 diff)

핵심 추가/삭제/치환 포인트를 불릿으로 요약

환경변수/DB/스키마/스타일은 미변경임을 명시

리스크와 회피책

잠재 영향(성능/호환/보안) 1-2개

회피/완화 방법(토글·가드·폴백 등)

롤백 계획

되돌릴 커밋/파일, 복구 명령 또는 수동 복원 절차(간단)

검증 시나리오 & 성공 기준

재현 기준: “에러 로그/증상 → 기대 결과” 단계별 제시

최소 3개 엔드투엔드 체크(예: 로그인, 파일 업로드, 관리자 CRUD)

성공 조건: “200 응답, 테이블 반영 확인” 등 수치/문구로 명확히

[제출 규칙]

위 사전 변경 안내가 승인되기 전에는 코드 본문 제시 금지

승인 후에는 파일 전체 최종본만 제출 (부분 코드 절대 금지)

수정 전/후 결과를 로그·스크린샷으로 검증해야 함

예상 변경 내용 (요약 diff)

핵심 추가/삭제/치환 포인트를 불릿으로 요약

환경변수/DB/스키마/스타일은 미변경임을 명시

리스크와 회피책

잠재 영향(성능/호환/보안) 1-2개

회피/완화 방법(토글·가드·폴백 등)

롤백 계획

되돌릴 커밋/파일, 복구 명령 또는 수동 복원 절차(간단)

검증 시나리오 & 성공 기준

재현 기준: “에러 로그/증상 → 기대 결과” 단계별 제시

최소 3개 엔드투엔드 체크(예: 로그인, 파일 업로드, 관리자 CRUD)

성공 조건: “200 응답, 테이블 반영 확인” 등 수치/문구로 명확히

[제출 규칙]

위 사전 변경 안내가 승인되기 전에는 코드 본문 제시 금지

승인 후에는 파일 전체 최종본만 제출 (부분 코드 절대 금지)

수정 전/후 결과를 로그·스크린샷으로 검증해야 함
