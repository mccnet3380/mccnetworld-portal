1) 수정 목적 (한 줄)

업무요청중 페이지에서 “업무요청중” 상태로 변경된 문서만 표시되도록 초기 필터 값을 고정.

2) 현재 상황 파악(요약)

현 페이지 초기 필터가 '대기,진행중'으로 설정되어 있어, 접수 관리에서 “업무요청중”으로 변경한 건이 목록에 나타나지 않음.

API는 GET /api/documents?activationStatus=... 형태로 쿼리 파라미터를 받는 구조(백엔드 변경 불필요).

3) 해결할 오류 / 재현 절차

현재 문제: 업무요청중 페이지가 '대기,진행중' 문서만 가져오고, “업무요청중”은 제외됨.

재현:

접수 관리 → 임의 문서 상태를 “업무요청중”으로 변경 → 저장(200 OK)

업무요청중 페이지 이동

방금 변경한 문서가 목록에 표시되지 않음

수정 후 기대:

업무요청중 페이지 최초 진입 시 activationStatus=업무요청중으로 패칭

방금 변경한 문서가 목록에 표시됨

4) 변경 범위 요약

백엔드: 변경 없음 ❌

프론트엔드: client/src/pages/WorkRequests.tsx 1개 파일만 수정 ✅

빌드/배포: 프론트 빌드 필요(npm run build), 서버 재시작 필요

명시: 환경변수(.env/Secrets), DB 연결, DB 스키마, UI(레이아웃/디자인/스타일) 모두 미변경.

5) 수정 파일 목록 (최소화)

client/src/pages/WorkRequests.tsx

변경 이유

초기 상태 필터를 '대기,진행중' → '업무요청중'으로 교체

해당 상태값이 실제 API 쿼리 파라미터로 정확히 전달되도록 확인(의존성/쿼리키 포함)

6) 예상 변경 내용 (요약 diff)

변경 전

const [statusFilter, setStatusFilter] = useState('대기,진행중');


변경 후

const [statusFilter, setStatusFilter] = useState('업무요청중');


보완(필요 시 내부 확인만)

페칭 훅/useEffect/useQuery의 의존성 및 쿼리 키에 statusFilter 포함 유지

실제 요청이 GET /api/documents?activationStatus=업무요청중으로 나가는지 encodeURIComponent 적용 확인

공백/철자/대소문자 오탈자 제거(정확히 “업무요청중”)

7) 핵심 추가/삭제/치환 포인트

치환: 초기 필터 값을 '업무요청중'으로 고정

유지: 레이아웃/색상/Tailwind 클래스/컴포넌트 구조 및 모든 UI 불변

유지: 백엔드 API 경로/스키마/쿼리 파라미터 명(activationStatus) 불변

8) 환경변수/DB/스키마/스타일 미변경 명시

✅ .env/Secrets 미변경

✅ DB 연결/운영 DB 로직 미변경

✅ DB 스키마 미변경

✅ 스타일/Tailwind/레이아웃/컴포넌트 구조 미변경

9) 리스크와 회피책

잠재 영향(호환성): 기존에 '대기,진행중' 상태 문서를 이 페이지에서 찾던 사용자 혼란 가능

회피: 운영 공지 또는 임시로 접수 관리에서 해당 문서의 상태를 “업무요청중”으로 일괄 전환

잠재 영향(데이터 품질): 일부 문서가 실제로는 “업무요청중”이어야 하는데 상태 미정리

회피: 접수 관리 화면에서 상태 클린업(대량 변경 절차), 이후 본 고정 필터 적용

10) 롤백 계획

파일 단위 복귀:

git checkout HEAD -- client/src/pages/WorkRequests.tsx
npm run build


이후 서버 재시작

11) 검증 시나리오 & 성공 기준

S1. 상태 변경

접수 관리 → 문서 상태 “업무요청중”으로 변경 → 저장

성공 기준: PUT /api/documents/{id}/activation-status 200 OK, Console 오류 0

S2. 목록 표시

업무요청중 페이지 이동

성공 기준: GET /api/documents?activationStatus=업무요청중 200 OK, 방금 변경한 문서 목록에 표시, Console 오류 0

S3. 타 상태 제외

동일 문서를 “개통/취소/폐기” 등으로 변경

성공 기준: 업무요청중 페이지에는 미표시, 각 전용 페이지로만 노출