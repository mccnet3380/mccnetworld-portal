사전 변경 안내(최종) — 개발서버 전용 테스트 후 운영 반영

⚠️ 중요: 본 변경은 개발 서버(Neon 연동) 에서만 작업·검증합니다.
운영 서버(Render 연동)에는 개발 서버에서 기능 검증 및 제 승인이 끝난 뒤 동일 변경을 적용합니다.
작업 중에는 운영 서버 코드/환경/DB 연결에 어떤 변경도 하지 않습니다.

[수정 원칙 - 필수]

오류 지점만 최소 수정, 불필요 변경 금지

UI(레이아웃/디자인/스타일/Tailwind/HTML 구조) 절대 변경 금지

환경(.env/Secrets/DB 연결/운영 DB 로직) 절대 변경 금지

DB 스키마 변경 금지(필요 시 ‘DB 스키마 변경 제안’으로만 설명)

부분 코드 금지 → 파일 전체 최종본 제출

테스트(개발) · 운영에서 동일 동작 보장

수정 후 서버 재시작 + 동일 시나리오 재검증 필수

[요구사항 요약]

메뉴 정책

근무자(admin/user): “접수 신청” 메뉴 숨김

판매점(dealer): “접수 신청” 메뉴 유지

근무자 원스톱 처리

“접수 관리(/documents)” 상단에 근무자 전용 ‘신규 접수’ 버튼 표시

버튼 클릭 시 기존 “접수 신청” 폼 컴포넌트를 모달로 재사용(UI/스타일 변경 금지)

근무자는 신규 접수 → 상태 변경(개통완료) 까지 한 화면에서 처리

권한별 조회(서버 가드 필수)

근무자: 전체 문서 조회 가능

판매점: 본인 문서만 조회(dealerId/접점코드/작성자 기준을 서버에서 강제 필터)

채팅 노출/권한

‘진행중’ 상태에서만 채팅 버튼 노출 및 접근 허용

다른 상태(대기/업무요청중/개통/기타완료/취소/폐기 등)는 채팅 버튼 미노출

서버에서도 문서 상태가 ‘진행중’일 때만 채팅 라우트/소켓 접근 허용(이중 가드)

[변경 범위 요약]
프론트(최소)

client/src/components/Sidebar.tsx

근무자(admin/user)에게 “접수 신청” 메뉴 숨김, 판매점(dealer)만 표시

client/src/pages/Documents*.tsx

근무자일 때 상단 ‘신규 접수’ 버튼 렌더(권한 가드)

채팅 버튼 노출 조건을 activationStatus === '진행중' 로 고정

client/src/pages/SubmitApplication.tsx (또는 동일 폼)

모달로 재사용(기능만 재사용, UI/스타일/구조 변경 금지)

백엔드(최소)

server/storage.ts(또는 대응 라우트/서비스)

getDocuments: userType별 필터(근무자=전체, 판매점=본인만)

createDocument: 근무자 생성 시 dealer 매핑/로그 보강

채팅 라우트/소켓: 문서 상태 ‘진행중’ 검증(미충족 시 403)

(참고) auth-routes.ts의 dealer-login은 현행 유지. 문제 시에만 minimal fix.

[예상 변경 포인트(개념, 요약 diff)]

Sidebar.tsx

const isDealer = !!user?.dealerId;

근무자(!isDealer && userType in ['admin','user']) → ‘접수 신청’ 메뉴 필터로 제거

Documents.tsx*

if (userType in ['admin','user']) → 상단 ‘신규 접수’ 버튼 렌더

채팅 버튼 조건:

{doc.activationStatus === '진행중' && (
  <Button ... onClick={() => handleChatOpen(doc.id)} />
)}


서버(채팅 가드)

채팅 관련 API/소켓 진입 시 문서 조회 → activation_status === '진행중' 아니면 403 반환

[리스크 & 회피책]

권한 누락으로 판매점이 타 판매점 문서 열람 위험 → 서버 필터 강제

채팅 조건 누락으로 다른 상태에서 노출 위험 → 프론트 가드 + 서버 가드 이중 적용

[롤백 계획]

프론트(메뉴/버튼/채팅)와 백엔드(필터/채팅가드) 분리 커밋

이슈 발생 시 해당 커밋만 되돌려 즉시 현 운영 상태 복귀

개발 서버에서만 롤백/재배포 반복 후 운영 반영은 승인 이후

[검증 시나리오 & 성공 기준] — 개발 서버에서만 실행

판매점 계정

사이드바에 ‘접수 신청’ 보임, 본인 문서만 조회

접수 제출 후 근무자 계정에서 해당 건 확인

‘진행중’ 상태일 때만 채팅 버튼 표시

근무자 계정

사이드바에 ‘접수 신청’ 없음

‘접수 관리’ 상단 ‘신규 접수’ 버튼 보임, 모달 폼으로 생성 성공

전체 문서 조회/상태변경 가능

‘진행중’ 상태에서만 채팅 버튼 표시

공통

개발 서버(Neon) 에서 콘솔/서버 에러 없음, UI 스타일 변화 없음

승인 후 동일 변경을 운영 서버(Render) 에 반영

제출 규칙

위 사전 변경 안내 승인 전에는 코드 본문 제시 금지

승인 후 파일 전체 최종본만 제출(부분 코드 절대 금지)

변경 전/후 동일 시나리오 스크린샷·로그로 개발 서버에서 검증 첨부