🛠️ 사전 변경 안내(개발 서버 전용) — 드롭다운 중복 실행 & 접점코드 자동매칭 실패 수정
✅ 변경 원칙(필수 준수)


오류가 발생한 부분만 수정 (불필요 변경 금지)


UI/디자인/CSS/Tailwind/HTML 구조 변경 금지


.env/Secrets/DB 연결/운영 DB 로직 변경 금지


DB 스키마 변경 금지


부분 코드 금지 → 파일 “전체 최종본” 제출


개발 서버(Neon)에서만 수정/테스트 → 검증 완료 후에만 운영 반영 제안


수정 후 동일 시나리오 재검증 및 로그 증빙 첨부



1) 해결할 오류 / 재현 절차(현재 상태)


증상 A: 통신사 선택 드롭다운이 한 번 선택 시 2회 이상 열리거나 onSelect가 중복 실행됨


증상 B: 통신사 선택 후 접점코드 자동매칭이 실패하고, 토스트에 “자동 매칭 오류: 접점코드 조회에 실패했습니다. 수동으로 입력해 주세요.”가 반복 노출됨


재현(개발 서버):


/submit 진입 → 통신사 자동완성에서 “후불)미디어어” 선택


우측 “개통방법 코드”는 여전히 빈값, 판매점명 미매핑


드롭다운이 이중으로 열림/닫힘하거나 API가 2회 이상 호출됨





2) 변경 범위 요약


프론트 전용 수정 + (필요 시) 백엔드 조회 조건 1곳 확인


운영 서버 코드/환경 절대 변경 금지


개발 서버(Neon) 연결에서만 테스트



3) 수정 파일 목록(최소화)


client/src/components/AutocompleteInput.tsx




이벤트 중복 실행 방지: onFocus 자동 open + onClick open 중복 / onChange → onSelect 체인 중복 호출 방지


디바운스/가드 플래그 도입(예: isOpenRef, selectingRef)으로 동일 선택 반복 방지




client/src/pages/SubmitApplication.tsx




자동매칭 단 한 번만 호출:


carrier 변경 시 lastCarrierId/isMatching 가드로 중복 호출 차단


성공 시 조용히 채움(토스트 금지), 실패 시 1회만 안내 토스트




판매점명 자동 채움: 접점코드 매칭 성공 시 storeName 입력란 자동 세팅


사이드바/레이아웃/스타일 변경 금지




(필요 시 확인만) server/storage.ts 또는 server/*routes*.ts의 접점코드 자동매칭 조회 함수




세션의 dealerId 사용이 정확한지 검증(예: users.username 기준이 아니라 session.user.dealerId 기준)


대소문자/공백 트림/접두어 규칙 일치 확인(L프, K프 등)


쿼리 1회만 실행되도록 보장(중복 트리거 방지)



⚠️ 백엔드 소스는 로직 보정이 필요한 경우에 한해 최소 수정. DB/스키마/환경 변수는 변경 금지.


4) 예상 변경 내용(요약 diff, 개념)


AutocompleteInput.tsx


onFocus에서 강제 open() 제거 또는 이미 open 상태면 무시


onClick/onChange/onSelect 간 중복 파이어 가드(selectingRef.current===true 시 리턴)


입력 디바운스(250~300ms)로 API 호출 빈도 관리




SubmitApplication.tsx


useRef로 isMatching, lastCarrierKey 보유 → 같은 carrier 재선택 시 호출 스킵


await autoMatchContactCode(carrierKey, session.dealerId) 1회만 호출


성공 시: 접점코드 + 판매점명 세팅, 토스트 미노출


실패 시: 1회 토스트 “매칭 결과 없음(수동 입력)”


API 에러 시: “자동매칭 호출 실패(일시적 장애)”




(필요 시) 서버


세션에서 dealerId 안전 취득 → dealer_id 필터 강제(WHERE dealer_id = ?)


캐리어키/접두어 규칙 일치 확인(트림/케이싱 통일)


중복 응답/두 번 실행 원인(라우트 중복 등록) 점검





5) 환경 고정(개발 서버 전용)


반드시 개발 DB(Neon) 로 연결되어 테스트해야 함


운영 서버(mccnetworld.com / Render) 반영 금지


PR/커밋 메시지에 [DEV ONLY] 접두어와 Neon 연결 스냅샷 로그 첨부



6) 리스크와 회피책


리스크: 자동완성 UX 민감(열림/닫힘 타이밍) → 과도한 디바운스로 지연 체감


회피: 디바운스 250~300ms 유지, 중복가드만 추가(UX 변화 최소화)


리스크: 세션 딜러 불일치 시 매칭 실패


회피: 로그아웃→재로그인 강제 배너(개발 환경에서만 단발 안내), 서버 로그로 세션의 dealerId 값 출력(개발 로그만)



7) 롤백 계획


수정 전 세 파일 전체 백업


이슈 발생 시 해당 2~3개 파일만 즉시 원복 → 개발 서버 재시작


원복 커밋에 [ROLLBACK DEV] 접두어 사용



8) 검증 시나리오 & 성공 기준(개발 서버)
시나리오 A: 드롭다운 중복 실행 방지


/submit → 통신사 입력칸 포커스


자동완성 제안 노출 → 항목 1회 클릭


기대 결과


드롭다운 1회만 열리고 닫힘


onSelect/매칭 API 1회만 호출(네트워크 탭/콘솔 로그로 증빙)




시나리오 B: 접점코드 자동매칭 성공


통신사 “후불)미디어어” 선택


기대 결과


매칭 API 1회 호출


개통방법 코드 자동 채움 + 판매점명 자동 채움


성공 토스트 없음(조용히 세팅)




시나리오 C: 접점코드 자동매칭 실패


매칭값이 없는 통신사 선택


기대 결과


실패 토스트 1회만 표시(중복 표시 금지)


입력란은 유지(수동 입력 가능)




시나리오 D: 세션/딜러 일치 검증


로그인 직후 /me 또는 콘솔에 session.dealerId 출력(개발 로그)


기대 결과


세션의 dealerId가 DB와 일치(예: 3)


이 값이 매칭 API에 동일 전달




성공 기준


네트워크 탭에서 통신사 선택 시 매칭 API 1회 호출만 보일 것


자동매칭 성공 시 입력란 자동 세팅 + 토스트 없음, 실패 시 1회 토스트


드롭다운 이중 열림/닫힘 현상 재현 불가



9) 증빙 로그(필수)


개발 콘솔/네트워크 캡쳐:


carrierSelected 타임스탬프 1회


autoMatchContactCode start/end 1회


session.dealerId 값 출력(개발 전용)





10) 제출 규칙


사전 변경 안내 승인 전, 코드 본문 제시 금지


승인 후 수정 파일 “전체 최종본” 만 제출


개발 서버에서 테스트/증빙 첨부 → 이후 운영 반영 PR 제안서 별도



요청 요약(한 줄)

개발 서버(Neon)에서만 AutocompleteInput.tsx와 SubmitApplication.tsx(+필요 시 서버 조회 조건 1곳) 최소 수정으로 드롭다운 중복 실행을 제거하고 접점코드 자동매칭을 1회 호출·정확 매핑되도록 고쳐주세요. 변경 후 위 성공 기준을 네트워크/콘솔 로그 캡쳐로 증명해 주세요. 운영 서버 코드는 절대 수정하지 마세요.
